// Set this when the frontend and backend are deployed as separate Render services.
// Example: window.HAKAYAA_SUBMIT_ENDPOINT = "https://hakayaa-backend.onrender.com/submit";
window.HAKAYAA_SUBMIT_ENDPOINT = window.HAKAYAA_SUBMIT_ENDPOINT || "";
